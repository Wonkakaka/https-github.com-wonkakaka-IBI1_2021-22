# Setting range and the starting amount.
number=10
sum=0
counter=1
# set the formula to make the program run as required.
while counter<=number:
  sum=sum+counter
# List result as need
print("sum=",sum) 
# The process is not over yet, all ten results need to be enumerated.
counter+=1
