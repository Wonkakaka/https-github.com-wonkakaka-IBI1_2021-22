# This is Return(1)
# Import numpy modules in python
import numpy as np
x=np.array([45,36,86,57,53,92,65,45])
print(np.sort(x))


# Return(2)
# Import numpy and matplotlib.pyplot modules in python
import numpy as np
import matplotlib.pyplot as plt
# First, I list his eight accomplishments.
n=8
marks=[45,36,86,57,53,92,65,45]
# Then I chang the shape of the boxplot by editing some parameters of the boxplot and get the average value.
plt.boxplot(marks,
            vert = False,
            whis = 3,
            patch_artist = False,
            meanline = True,
            showmeans = True,
            showbox = True,
            showcaps = True,
            showfliers = True,
            notch = False,
            labels = None
                )
# and name a boxplot from the data and the parameters 
plt.title('IBI1 practical marks of Rob')	
plt.show()


# Return(3)
import numpy as np
arr = [45,36,86,57,53,92,65,45]
a=np.mean(arr)
# According to the question, I compare the average with the pass line and output a different result.
if a<60:
        print("Fail")
else:
        print("Pass")
